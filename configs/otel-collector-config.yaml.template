receivers:
  otlp:
    protocols:
      grpc: {}
      http: {}

exporters:
  otlp/tempo:
    endpoint: "<TEMPO_HOST>:4317"
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp/tempo]
