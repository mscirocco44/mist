version: "3.8"
services:
  prometheus:
    image: prom/prometheus:latest
    restart: unless-stopped
    user: "0"
    volumes:
      - ./prometheus:/etc/prometheus:Z
    ports:
      - "9090:9090"

  # Grafana is always deployed as part of the stack.
  grafana:
    image: grafana/grafana:latest
    restart: unless-stopped
    user: "0"
    volumes:
      - ./grafana:/var/lib/grafana:Z
    ports:
      - "3000:3000"

  # Loki and Tempo are profile-gated so they only start when enabled.
  loki:
    profiles: ["loki"]
    image: grafana/loki:2.8.2
    restart: unless-stopped
    user: "0"
    volumes:
      - ./loki:/etc/loki:Z
    ports:
      - "3100:3100"

  tempo:
    profiles: ["tempo"]
    image: grafana/tempo:1.5.0
    restart: unless-stopped
    user: "0"
    volumes:
      - ./tempo:/etc/tempo:Z
    ports:
      - "3200:3200"
      - "4317:4317"
